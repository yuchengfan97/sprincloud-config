server:
  port: 80

eureka:
  client:
    register-with-eureka: true
    service-url:
      defaultZone:  http://localhost:7001/eureka,http://localhost:7002/eureka  #集群版


ribbon:
  ReadTimeout:  5000
  ConnectTimeout: 5000
logging:
  level:
    com.iss.springcloud.service.PaymentFeignService: debug
